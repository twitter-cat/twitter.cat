# twitter.cat

## development

### client

1. set your server url in `client/js/config.js`
2. `cd client && bunx serve`

### server

1. rename `server/.env.example` to `server/.env`
2. set your postgres credentials in `server/.env`
3. `cd server`
4. `bun install`
5. `bun run dev`