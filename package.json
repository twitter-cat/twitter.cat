{
  "scripts": {
    "server:dev": "cd server && bun dev",
    "client:dev": "cd client && bun ./**/*.html --console",
    "client:push": "git push origin `git subtree split --prefix client main`:gh-pages --force"
  }
}
